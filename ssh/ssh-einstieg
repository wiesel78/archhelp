# RSA-KEY erstellen...

ssh-keygen -t rsa


# RSA-KEY auf das Zielsystem überspielen
# user = der Benutzer auf dem Zielsystem
# port = der Port auf dem ssh auf dem Zielsystem hörcht

ssh-copy-id -i ./.ssh/id_rsa.pub '-p port user@host'
