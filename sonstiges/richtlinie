-loadkeys de
-partitionieren
	root
	home
	swap

-formatieren
-mounten nach /mnt 
-pacstrap /mnt base base-devel 
	pacstrap /mnt grub-bios btrfs-progs 
	pacstrap /mnt wireless_tools wpa_supplicant

-genfstab -Lp /mnt >> /mnt/etc/fstab
-arch-chroot /mnt
-passwd
-grub-install /dev/sdX
-/boot/grub/grub.cfg bearbeiten falls nötig
-systemd conf (archhelp autoconf)
-mkinitcpio -p linux
-(archhelp/programme) programmliste anpassen 
-(archhelp/programme) ./configure und make ausführen 
-systemctl enable *
    avahi-daemon.service
    cups.service
    gdm.service
    NetworkManager.service
    ntpd.service

reboot
