# enthaelt 32bit-packete fuer 64bit systeme
# install
# /etc/pacman.conf oeffnen und folgendes hinzufuegen bzw. auskommentieren

[multilib]
Include = /etc/pacman.d/mirrorlist

# dann nur noch 

sudo pacman -Syy


