# um Arch auf einen USB-Stick zu installieren
# installiert man Arch wie sonst auch aber muss 
# vor dem ersten boot "usb" direkt nach "udev"
# im HOOKS-Array der /etc/mkinitcpio.conf eintragen

HOOKS="base udev usb autodetect pata scsi sata btrfs filesystems 
usbinput"


# fuer grub ist zu beachten dass der USB-Stick, von dem gebootet
# wird, mit (hd0,0) angesprochen wird...
